export interface Trick {
  name: string;
  trickType: string;  
  difficulty: string;  
  description: string;
  variation: string;
  tutorialLinks: string[];
  status: string;
  trickId: number;  
}
